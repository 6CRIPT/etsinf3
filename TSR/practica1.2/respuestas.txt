1.2.1.1) En el a) yo diria que solo 1 al igual que en el b). En el caso del c) diria que igualmente 1 por fichero.
1.2.1.2) Para crear un modulo que en este caso se vaya a usar en otro programa, hay que usar los 'exports' que hace abajo, es decir definimos las funciones que queremos exportar y luego declaramos que las exportamos, entonces luego podremos asignar el modulo a una constante de nombre x y utilizar sus metodos como x.metodo()
        la diferencia puede radicar en que aqui no se programa ninguna interfaz, que se hace un catch en las excepciones...
1.2.1.3) esto es codigo, esta en esta carpeta.

1.2.2.1)
